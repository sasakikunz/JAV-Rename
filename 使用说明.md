# 影片番号规范化工具 v4.0 - 使用说明

## 功能概述

本工具用于规范化日本影片番号，支持以下核心功能：

1. **规则学习** - 从Excel文件学习影片前缀规则并缓存
2. **目录文件名规范化** - 扫描目录下文件和文件夹，生成规范化Excel
3. **Excel文件名规范化** - 读取Excel中的文件名，规范化后填入第二列
4. **文件匹配** - 将规范化名称与电影目录模糊匹配
5. **批量重命名** - 根据Excel进行实际重命名
6. **增强型批量重命名** - 交互式重命名为上级文件夹名，递归处理子文件夹

---



## 安装依赖

```bash
pip install pandas openpyxl
```

---

## 命令详解

### 1. learn（规则学习）

**功能**：从Excel文件学习影片前缀规则并缓存到本地

**命令格式**：
```bash
python3 main.py learn --excel <Excel文件路径> [--cache <缓存文件路径>]
```

**参数说明**：
| 参数 | 简写 | 是否必须 | 默认值 | 说明 |
|------|------|----------|--------|------|
| `--excel` | `-e` | **是** | - | Excel映射文件路径，至少包含两列（原文件名、规范化后文件名） |
| `--cache` | `-c` | 否 | `prefix_cache.json` | 缓存文件路径 |

**使用示例**：
```bash
python3 main.py learn -e mapping.xlsx
python3 main.py learn -e data/mapping.xlsx -c data/prefix_cache.json
```

---

### 2. normalize（目录文件名规范化）

**功能**：扫描目录下所有文件和文件夹，规范化名称后生成Excel报告

**命令格式**：
```bash
python3 main.py normalize --folder <目录路径> [--output <输出路径>] [--cache <缓存路径>] [--recursive]
```

**参数说明**：
| 参数 | 简写 | 是否必须 | 默认值 | 说明 |
|------|------|----------|--------|------|
| `--folder` | `-d` | **是** | - | 要扫描的文件夹路径 |
| `--output` | `-o` | 否 | 自动生成 | 输出Excel文件路径 |
| `--cache` | `-c` | 否 | `prefix_cache.json` | 缓存文件路径 |
| `--recursive` | `-r` | 否 | False | 递归扫描所有子目录 |

**使用示例**：
```bash
python3 main.py normalize -d /home/user/videos
python3 main.py normalize -d /home/user/videos -r -o result.xlsx
```

---

### 3. normalize-excel（Excel文件名规范化）

**功能**：读取Excel中的文件名（第一列），规范化后填入第二列

**命令格式**：
```bash
python3 main.py normalize-excel --input <Excel文件路径> [--output <输出路径>] [--cache <缓存路径>]
```

**参数说明**：
| 参数 | 简写 | 是否必须 | 默认值 | 说明 |
|------|------|----------|--------|------|
| `--input` | `-i` | **是** | - | 输入Excel文件路径，第一列包含要规范化的文件名 |
| `--output` | `-o` | 否 | 自动生成 | 输出Excel文件路径 |
| `--cache` | `-c` | 否 | `prefix_cache.json` | 缓存文件路径 |

**使用示例**：
```bash
python3 main.py normalize-excel -i filenames.xlsx
python3 main.py normalize-excel -i input.xlsx -o normalized.xlsx
```

---

### 4. match（模糊匹配）

**功能**：将规范化后的名称与电影目录进行模糊匹配，输出匹配度

**命令格式**：
```bash
python3 main.py match --input <规范化文件> --movie-list <电影目录> [--output <输出路径>] [--threshold <阈值>]
```

**参数说明**：
| 参数 | 简写 | 是否必须 | 默认值 | 说明 |
|------|------|----------|--------|------|
| `--input` | `-i` | **是** | - | 包含规范化名称的Excel文件 |
| `--movie-list` | `-m` | **是** | - | 电影目录Excel文件，第一列包含电影名称 |
| `--output` | `-o` | 否 | 自动生成 | 输出匹配结果的Excel文件路径 |
| `--threshold` | `-t` | 否 | `0.7` | 匹配相似度阈值（0-1） |

**使用示例**：
```bash
python3 main.py match -i normalized.xlsx -m movies.xlsx
python3 main.py match -i normalized.xlsx -m movies.xlsx -t 0.8 -o matched.xlsx
```

---

### 5. rename（批量重命名）

**功能**：根据Excel表格中的映射关系批量重命名文件

**命令格式**：
```bash
python3 main.py rename --folder <目录路径> --input <Excel文件> [--output <输出路径>] [--dry-run]
```

**参数说明**：
| 参数 | 简写 | 是否必须 | 默认值 | 说明 |
|------|------|----------|--------|------|
| `--folder` | `-d` | **是** | - | 要重命名的文件夹路径 |
| `--input` | `-i` | **是** | - | 包含重命名映射的Excel文件 |
| `--output` | `-o` | 否 | - | 输出重命名结果的Excel文件路径，只输入目录不输入文件名则在目录下自动生成带时间戳的文件，不输入则不生成输出文件 |
| `--dry-run` | - | 否 | False | 预览模式，不实际执行 |

**使用示例**：
```bash
python3 main.py rename -d /home/user/videos -i normalized.xlsx --dry-run
python3 main.py rename -d /home/user/videos -i normalized.xlsx
```

---

### 6. rename-enhanced（增强型批量重命名）

**功能**：交互式批量重命名，进入每个文件夹列出所有项目供用户选择重命名为上级文件夹名，可选自动删除未重命名的文件

**命令格式**：
```bash
python3 main.py rename-enhanced --folder <根目录路径> [--output <日志路径>] [--remove]
```

**参数说明**：
| 参数 | 简写 | 是否必须 | 默认值 | 说明 |
|------|------|----------|--------|------|
| `--folder` | `-d` | **是** | - | 根文件夹路径 |
| `--output` | `-o` | 否 | - | 日志导出路径（支持.xlsx或.log格式），不输入则当前目录生成日志文件，输入地址则生成log文件，输入地址和文件名则指定文件类型 |
| `--remove` | `-r` | 否 | 关闭 | 删除模式：重命名后自动删除当前文件夹内所有未被重命名的文件和子文件夹 |

**工作流程**：

1. 遍历根目录下每个子文件夹，列出内部所有文件和子文件夹，按文件大小从大到小排列
2. 提示用户选择要重命名的项目编号：
   - 选择文件→保留原扩展名，重命名为上级文件夹名
   - 选择子文件夹→重命名为上级文件夹名，并递归进入处理
   - 输入0 → 跳过当前文件夹
3. 当使用`--remove`参数时，完成重命名后自动提示删除该文件夹内所有未被重命名的项目（需用户输入`y`确认）
4. 自动记录所有操作日志，支持导出供后续追溯

**使用示例**：
```bash
python3 main.py rename-enhanced -d /home/user/videos
python3 main.py rename-enhanced -d /home/user/videos -o rename_log.xlsx
python3 main.py rename-enhanced -d /home/user/videos --remove
python3 main.py rename-enhanced -d /home/user/videos -r -o rename_log.xlsx
```

**操作界面示例（普通模式）**：
```
  ==================================================
  文件夹: ORE-798
  ==================================================
    1.abs082.wmv (12.4MB)
    2.abs082.jpg (1.2MB)
    3.hkkane@18p2p  [文件夹]
    4.hkkane@18p2p.txt (0.5KB)
    ----------------------------------------
    0.不修改

  请输入需要重命名的项目编号(1-4)，输入0表示不修改: 2
    abs082.jpg -> ORE-798.jpg [成功]
```

**操作界面示例（删除模式 `--remove`）**：
```
  ==================================================
  文件夹: ORE-798
  ==================================================
    1.abs082.wmv (12.4MB)
    2.abs082.jpg (1.2MB)
    3.hkkane@18p2p  [文件夹]
    4.hkkane@18p2p.txt (0.5KB)
    ----------------------------------------
    0.不修改（将删除所有未重命名的文件）

  请输入需要重命名的项目编号(1-4)，输入0表示不修改: 2
    abs082.jpg -> ORE-798.jpg [成功]

    以下项目未被重命名，将被删除:
      abs082.wmv (12.4MB)
      hkkane@18p2p.txt (0.5KB)
      hkkane@18p2p  [文件夹]

    确认删除以上 3 个项目？(y/n): y
      abs082.wmv [已删除]
      hkkane@18p2p.txt [已删除]
      hkkane@18p2p [已删除]
```

## 完整工作流示例

```bash
# 1. 学习规则
python3 main.py learn -e mapping.xlsx -c prefix_cache.json

# 2. 规范化目录文件名（递归扫描）
python3 main.py normalize -d /home/user/videos -r -o normalized.xlsx

# 3. （可选）规范化Excel中的文件名
python3 main.py normalize-excel -i filenames.xlsx -o normalized_from_excel.xlsx

# 4. 模糊匹配电影目录
python3 main.py match -i normalized.xlsx -m movies.xlsx -o matched.xlsx

# 5. 批量重命名（先预览再执行）
python3 main.py rename -d /home/user/videos -i normalized.xlsx --dry-run
python3 main.py rename -d /home/user/videos -i normalized.xlsx

# 6. 增强型批量重命名（交互式）
python3 main.py rename-enhanced -d /home/user/videos -o rename_log.xlsx
```

---

## Excel文件格式要求

### learn 命令输入格式
| 列A（原文件名） | 列B（规范化后文件名） |
|----------------|----------------------|
| messy_name.mp4 | PREFIX-123.mp4 |
| another_file.mkv | ABC-456.mkv |

### normalize 命令输出格式
| 原名称 | 规范名称 | 类型 | 路径 |
|--------|----------|------|------|
| movie1.mp4 | PREFIX-001.mp4 | file | /path/to/movie1.mp4 |
| folder1 | FOLDER1 | folder | /path/to/folder1 |

### normalize-excel 命令输入格式
| 列A（文件名） |
|--------------|
| messy_file.mp4 |
| another_name.mkv |

### match 命令输出格式
| 原名称 | 规范名称 | 匹配名称 | 匹配度 |
|--------|----------|----------|--------|
| movie1.mp4 | PREFIX-001.mp4 | 电影A | 0.85 |
| movie2.mkv | ABC-456.mkv | 电影B | 0.92 |

---

## 注意事项

1. **批量重命名前务必备份**，建议先使用 `--dry-run` 预览
2. 匹配功能需要用户提供标准电影名称目录Excel
3. 缓存文件需要妥善管理，避免规则混乱
4. 递归扫描会遍历所有层级的子目录
5. Excel文件建议使用 `.xlsx` 格式

---

## 项目结构

```
test/
├── main.py                    # 入口文件
├── cli/
│   ├── commands.py            # CLI命令定义
│   └── __init__.py
├── config/
│   ├── settings.py            # 配置管理类
│   └── __init__.py
├── core/
│   ├── normalizer.py          # 番号规范化核心类
│   ├── matcher.py             # 模糊匹配核心类
│   ├── cache_manager.py       # 缓存管理类
│   └── __init__.py
├── fileio/
│   ├── excel_reader.py        # Excel读取器
│   ├── excel_writer.py        # Excel写入器
│   ├── file_scanner.py        # 文件扫描器
│   └── __init__.py
└── 使用说明.md                # 本说明文档
```